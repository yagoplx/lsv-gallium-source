# Cleans logs automatically
# Part of yagoply's LSV-Gallium Utilities

set -a
skip="true"
dirnow=$(pwd)
cd /home/mcs/snapServer/twf

sudo -Eu mcs /home/mcs/snapServer/twf/watchdog cleanlogs
cd $dirnow
exit 0